# Pseudocode

function main takes no arguments and returns nothing
    display the header for the program
    create an empty list call it comp_times
    initialize time_as_float to 0.0
    because the program is supposed to quit on -1
    create a main while loop that runs until time_as_float equals -1.0
        